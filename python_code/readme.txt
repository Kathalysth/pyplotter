How to run the program:

1. Ensure you have python installed.
2. install the following Packages
    i. scipy-to install run - pip install scipy
    ii. matplotlib - to install run - pip install scipy
    iii. numpy - to install run - pip install numpy

